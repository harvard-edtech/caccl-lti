const chai = require('chai')
const expect = chai.expect;
const genSignature = require('../genSignature.js');

describe('genSignature', function () {
  it('Generates test signatures correctly', function () {

  });
});
